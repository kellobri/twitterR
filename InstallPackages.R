# Install Packages

install.packages(c('twitteR','tm','SnowballC','stringr','wordcloud'))

# Use the Add packages function from shiny-dev
# addPackages <- function(pkgs) {
#  install.packages(pkgs, '/usr/lib64/R/library')
# }
